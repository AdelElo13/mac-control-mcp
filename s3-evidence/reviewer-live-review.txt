REVIEW RESULT for your S3 branch (the reviewer HAS desktop access and ran your LIVE checks; your uncommitted work was committed by the reviewer — continue with new commits; if `git commit` fails on index.lock, leave changes uncommitted and say so).
A5 ground precision: DONE — all six baseline labels now correct live (Untitled→AXStaticText value; Recents/Shared/Add User…/account correct, wrong hits demoted to alternatives). A7: DONE (Safari js_error, Chrome permission_policy_denied, both with hints). Minor: BrowserDOMController.swift:151-158 generic js_error hint says "check Allow JavaScript from Apple Events" even when Apple Events worked and the page's CSP blocked eval — differentiate the two hints.
C2 element_at_point: Finder 70/71 = 98.6% same id (DONE). System Settings 16/18 = 88.9% (target ≥90%): the two misses pick a coarser sibling (AXCell / AXOutline "Sidebar") over the annotated leaf. Check GeometricHitTest.search (GeometricHitTest.swift:34-56): likely the true leaf is excluded by AXPayload.isInteractive (row cells / outline rows not flagged interactive) — widen the predicate for AXCell/AXRow inside outlines/tables or prefer the deepest containing interactive element over the smallest-area one when areas are within 10%. Add a unit test with a synthetic outline where the leaf must win.
B5 OCR speed: WRONG — a REGRESSION. Measured live 7 runs: ground(target "Downloads", strategy ocr, Finder window) p50 = 1116 ms vs 0.9.0 baseline 948 ms (target ≤400). ocr_screen fast is genuinely fast now (p50 158 ms) and accurate unchanged (947 ms). Root cause: ScreenController.groundingOCR (ScreenController.swift:546-554) runs the fast pass and then, whenever no candidate reaches confidence ≥0.8 (GroundingPolicy.needsAccurate, GroundingPolicy.swift:81), runs the accurate pass IN ADDITION and merges blocks; GroundingPolicy.ocrConfidence (GroundingPolicy.swift:72-79) caps an exact match at min(0.9, recognition) where recognition is Vision's per-block confidence of the fast pass — measured 0.72 for real UI text — so the common case pays fast+accurate. Also GroundingController.swift:319 only skips OCR when axCandidates.count == 1; with two equally-confident correctly-ranked AX ties (strategy auto) a full OCR pass still runs (measured 2141 ms). Required: (1) fall back to accurate ONLY when the target string was not found at all in the fast pass (an exact/normalised text match at any recognition confidence is a hit; recognition confidence may lower the reported confidence but must not trigger a second Vision pass); (2) skip OCR in strategy auto whenever the AX top candidate has confidence 1.0 (or is strictly ahead of the runner-up), not only when there is exactly one candidate; (3) rewrite GroundingOCRPassTests.weakFastMatchFallsBackAndIsRetained so it asserts the new rule (fast exact match → no accurate pass), and add a test that a not-found fast pass DOES fall back; (4) update s3-evidence/REPORT.md: B5 is now verified and must show the new expected numbers (reviewer will re-measure: targets ground ocr Finder ≤400 ms, Safari/Chrome ≤100 ms, auto with AX hit ≤ AX time + 5 ms).
Run the named suites again and report RED→GREEN for the new tests.
