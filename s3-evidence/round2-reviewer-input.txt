REVIEW ROUND 2 for S3 (reviewer re-measured live; your fixes were committed by the reviewer as 8866f1b — continue with new commits; if index.lock blocks you, leave uncommitted and say so).
DONE: B5 ground ocr Finder p50 157 ms (was 1116; ≤400 target), Safari 82 ms, Chrome 108–114 ms (≈ the Vision fast floor for that window — accepted), auto skips OCR (424 ms = AX time), A5 6/6 still correct, C2 System Settings 17/17 = 100%, A7 hint correct.
WRONG (new, must fix): element_at_point on Finder list-view column-header buttons ("Size" at (1444.5,105), "Kind" at (1550.5,105)) is NON-DETERMINISTIC: 3/8 runs return an AXCell of a row scrolled under the floating header (el_… AXRow cell at (213,96) 1587×28) instead of the AXButton; the pre-fix build returned the button. Runs that return the cell take 1047–1092 ms, runs that return the button 629–938 ms. Root cause: GeometricHitTest.swift:42 `deadline = Date().addingTimeInterval(1)` truncates the DFS; the outline's rows/cells are visited before the header AXGroup, and because AXRow/AXCell became selectable in collections (GeometricHitTest.swift:50-51) a truncated walk yields a WRONG cell instead of the old "container" fallback. Finder agreement dropped 98.6% → 97.2% and the 71-call sweep went 3119 → 6209 ms.
Required: (1) GeometricHitTest.walk child loop (GeometricHitTest.swift:59-62): visit children whose frame contains the point FIRST (stable partition), and do not descend into AXRow/AXCell children whose frame is non-nil, ≥2×2 and does not contain the point (keep the coarse-geometry exemption only for nil/degenerate frames) — the header group must then be reachable in a few dozen reads; (2) track truncation (deadline or node cap); if truncated and the only candidates are AXRow/AXCell, return nil → hit_test_quality "container" (or a new "partial" quality with a hint to retry), never a row cell from a cut-off walk; (3) prefer the deepest/smallest interactive element that contains the point among those found — a floating header button must beat a row cell whose frame merely overlaps; (4) unit test in GeometricHitTestTests: outline with N rows (row cell frame containing the point) plus a later-sibling header AXGroup→AXButton; with nodeCap smaller than the row count the result must be the button or nil, never the cell; (5) target: element_at_point on those header points ≤150 ms and deterministic (8/8 AXButton) — write the exact probe into LIVE-CHECKS.md.
Run GeometricHitTestTests, ElementAtPointTests, GroundingPrecisionTests, GroundingOCRPassTests, Phase9ToolsTests and report RED→GREEN.
